import { PageHeader, PageShell } from '../components/PageShell'

export default function Scouting() {
  return (
    <PageShell>
      <PageHeader
        title="Scouting Report"
        subtitle="Per-90 percentiles vs positional peers — compare up to 4 players side by side"
      />
      <p className="text-ink-2">Coming in Phase 5.</p>
    </PageShell>
  )
}
